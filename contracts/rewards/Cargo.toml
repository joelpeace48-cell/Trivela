[package]
name = "trivela-rewards-contract"
version = "0.1.0"
edition = "2021"
publish = false
rust-version = "1.75.0"
description = "Trivela on-chain rewards and points contract"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[features]
testutils = ["soroban-sdk/testutils"]
# Off by default: poseidon.rs/merkle.rs need ark_bn254 + light_poseidon, which
# aren't vetted/pinned here yet. See the #[cfg] comment in src/lib.rs.
poseidon-experimental = []

# `kani` is set only under the Kani formal-verification harness runner; declare
# it so the compiler doesn't flag it as an unexpected cfg under -D warnings.
[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }

[dependencies]
soroban-sdk = "25.1"

[dev-dependencies]
soroban-sdk = { version = "25.1", features = ["testutils"] }
trivela-campaign-contract = { path = "../campaign" }
ed25519-dalek = "2"
proptest = "1.4"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true

[profile.release-with-logs]
inherits = "release"
debug-assertions = true
