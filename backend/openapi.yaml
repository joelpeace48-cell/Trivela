openapi: 3.1.0
info:
  title: Trivela API
  version: 0.1.0
  description: |
    REST API for the Trivela campaign and rewards platform. Handles campaign metadata,
    health checks, and Soroban RPC configuration for the frontend.

    See the [API Migration Guide](../docs/API_MIGRATION.md) for v0 → v1 breaking changes,
    endpoint changelog, deprecation policy, and migration examples with curl snippets.
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html

servers:
  - url: http://localhost:3001
    description: Local development server
  - url: https://api.trivela.example.com
    description: Production server

tags:
  - name: Health
    description: Health check and monitoring endpoints
  - name: Campaigns
    description: Campaign management operations
  - name: Config
    description: Configuration and metadata endpoints
  - name: Audit
    description: Audit log endpoints
  - name: Admin
    description: Administrative API key management
  - name: Organizations
    description: Organization and team member management
  - name: IndexRead
    description:
      'Public read API over indexed Soroban event data (cursor-paginated, ETag-cached) — #560'
  - name: StellarToml
    description: 'SEP-1 stellar.toml reward-token metadata — #551'
  - name: SponsoredAccounts
    description: 'CAP-33 Stellar reserve sponsorship for new accounts — #556'
  - name: ClaimableBalances
    description: 'Claimable balance lifecycle for unclaimed/expired rewards (CAP-23) — #548'

security:
  - ApiKeyAuth: []
  - BearerAuth: []

paths:
  /health:
    get:
      summary: Check API and RPC health
      tags:
        - Health
      operationId: getHealth
      security: []
      responses:
        '200':
          description: Health status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'

  /health/rpc:
    get:
      summary: Check Soroban RPC health
      tags:
        - Health
      operationId: getRpcHealth
      security: []
      responses:
        '200':
          description: RPC is healthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RpcHealthResponse'
        '503':
          description: RPC is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RpcHealthResponse'

  /metrics:
    get:
      summary: Get Prometheus metrics
      tags:
        - Health
      operationId: getMetrics
      security: []
      responses:
        '200':
          description: Prometheus-format metrics
          content:
            text/plain:
              schema:
                type: string

  /api/v1:
    get:
      summary: Get API information
      tags:
        - Config
      operationId: getApiInfo
      security: []
      responses:
        '200':
          description: API metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiInfo'

  /api/v1/config:
    get:
      summary: Get public configuration
      tags:
        - Config
      operationId: getConfig
      security: []
      responses:
        '200':
          description: Public configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigResponse'

  /api/v1/explorer:
    get:
      summary: Get explorer links
      tags:
        - Config
      operationId: getExplorerLinks
      security: []
      responses:
        '200':
          description: Explorer configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExplorerResponse'

  /api/v1/campaigns:
    get:
      summary: List campaigns
      tags:
        - Campaigns
      operationId: listCampaigns
      security: []
      parameters:
        - name: page
          in: query
          description: Page number (1-indexed)
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: limit
          in: query
          description: Items per page
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
        - name: offset
          in: query
          description: Number of items to skip
          schema:
            type: integer
            minimum: 0
        - name: active
          in: query
          description: Filter by active status
          schema:
            type: string
            enum: [true, false]
        - name: q
          in: query
          description: Full-text search query (supports phrases, prefix with *, boolean AND/OR/NOT)
          schema:
            type: string
        - name: tags
          in: query
          description: Comma-separated tags (any match includes campaign)
          schema:
            type: string
        - name: category
          in: query
          description: Filter by category
          schema:
            type: string
        - name: sort
          in: query
          description: Sort field
          schema:
            type: string
        - name: order
          in: query
          description: Sort order
          schema:
            type: string
            enum: [asc, desc]
        - name: status
          in: query
          description: >-
            Filter by campaign status. Defaults to `published` for public (no API key) requests.
            Requesting `draft`, `archived`, or `all` requires an API key.
          schema:
            type: string
            enum: [draft, published, archived, all]
      responses:
        '200':
          description: Paginated campaign list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CampaignListResponse'
        '401':
          description: API key required to access draft, archived, or all campaigns
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    post:
      summary: Create a new campaign
      tags:
        - Campaigns
      operationId: createCampaign
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CampaignCreate'
      responses:
        '201':
          description: Campaign created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Campaign'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Slug conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/campaigns/{id}:
    get:
      summary: Get campaign by ID
      tags:
        - Campaigns
      operationId: getCampaignById
      security: []
      parameters:
        - name: id
          in: path
          required: true
          description: Campaign ID
          schema:
            type: string
      responses:
        '200':
          description: Campaign details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Campaign'
        '404':
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    put:
      summary: Update campaign
      tags:
        - Campaigns
      operationId: updateCampaign
      parameters:
        - name: id
          in: path
          required: true
          description: Campaign ID
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CampaignUpdate'
      responses:
        '200':
          description: Campaign updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Campaign'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    delete:
      summary: Delete campaign
      tags:
        - Campaigns
      operationId: deleteCampaign
      parameters:
        - name: id
          in: path
          required: true
          description: Campaign ID
          schema:
            type: string
      responses:
        '204':
          description: Campaign deleted
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/campaigns/{id}/stats:
    get:
      summary: Get campaign analytics and stats
      tags:
        - Campaigns
      operationId: getCampaignStats
      security: []
      parameters:
        - name: id
          in: path
          required: true
          description: Campaign ID
          schema:
            type: string
        - name: range
          in: query
          description: Time range (7d, 30d, all)
          schema:
            type: string
            enum: [7d, 30d, all]
            default: 7d
        - name: from
          in: query
          description: Start date (ISO 8601)
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          description: End date (ISO 8601)
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Campaign statistics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CampaignStats'
        '404':
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/campaigns/by-slug/{slug}:
    get:
      summary: Get campaign by slug
      tags:
        - Campaigns
      operationId: getCampaignBySlug
      security: []
      parameters:
        - name: slug
          in: path
          required: true
          description: Campaign slug
          schema:
            type: string
      responses:
        '200':
          description: Campaign details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Campaign'
        '404':
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/campaigns/{id}/image:
    post:
      summary: Upload campaign image
      tags:
        - Campaigns
      operationId: uploadCampaignImage
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - image
              properties:
                image:
                  type: string
                  format: binary
      responses:
        '200':
          description: Image uploaded
          content:
            application/json:
              schema:
                type: object
                properties:
                  imageUrl:
                    type: string
                    format: uri
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Campaign not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/categories:
    get:
      summary: List categories with campaign counts
      tags:
        - Campaigns
      operationId: listCategories
      security: []
      responses:
        '200':
          description: Category list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        count:
                          type: integer

  /api/v1/tags:
    get:
      summary: List tags with frequency counts (top 50)
      tags:
        - Campaigns
      operationId: listTags
      security: []
      responses:
        '200':
          description: Tag list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        count:
                          type: integer

  /api/v1/admin/api-keys:
    get:
      summary: List API keys (metadata only)
      tags:
        - Admin
      operationId: listApiKeys
      responses:
        '200':
          description: API key list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/ApiKeyMetadata'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
    post:
      summary: Create a new API key
      tags:
        - Admin
      operationId: createApiKey
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                label:
                  type: string
                expiresAt:
                  type: string
                  format: date-time
                  nullable: true
                orgId:
                  type: string
                  description: Scope the key to an organization.
                scopes:
                  type: array
                  items:
                    $ref: '#/components/schemas/ApiKeyScope'
                  description: Defaults to the full legacy scope set if omitted.
                rateTier:
                  $ref: '#/components/schemas/ApiKeyRateTier'
                  description: Defaults to `standard` if omitted.
      responses:
        '201':
          description: API key created (raw key returned once)
          content:
            application/json:
              schema:
                type: object
                properties:
                  key:
                    type: string
                  metadata:
                    $ref: '#/components/schemas/ApiKeyMetadata'

  /api/v1/admin/api-keys/{id}:
    delete:
      summary: Revoke an API key
      tags:
        - Admin
      operationId: revokeApiKey
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Key revoked
        '404':
          description: Key not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/admin/api-keys/{id}/rotate:
    put:
      summary: Rotate an API key (revoke old, issue new)
      tags:
        - Admin
      operationId: rotateApiKey
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: New key issued
          content:
            application/json:
              schema:
                type: object
                properties:
                  key:
                    type: string
                  metadata:
                    $ref: '#/components/schemas/ApiKeyMetadata'

  /api/v1/admin/api-keys/{id}/rate-tier:
    put:
      summary: Update an API key's rate tier
      description: >-
        Changes the enforced rate-limit tier in place, without rotating (and thus invalidating) the
        caller's existing credential.
      tags:
        - Admin
      operationId: updateApiKeyRateTier
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - rateTier
              properties:
                rateTier:
                  $ref: '#/components/schemas/ApiKeyRateTier'
      responses:
        '200':
          description: Rate tier updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  metadata:
                    $ref: '#/components/schemas/ApiKeyMetadata'
        '400':
          description: Invalid rate tier value
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '404':
          description: Key not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/pii/export-user:
    post:
      summary: Export all off-chain PII for a wallet address or email (GDPR right of access)
      description: >-
        Returns every row across off-chain tables (participation, credits/claims, referrals,
        notification preferences, push subscriptions, etc.) matching the given identifier. Web Push
        credential fields (`p256dh`, `auth`) are redacted. See `docs/EVENT_SCHEMA.md` for the
        on-chain immutability notes this endpoint is scoped by.
      tags:
        - Admin
      operationId: exportPiiForUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - identifier
              properties:
                identifier:
                  type: string
                  description: Wallet address (G...) or email to export data for.
      responses:
        '200':
          description: Export payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PiiExportResponse'
        '400':
          description: Missing identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/pii/purge-user:
    post:
      summary: Erase all off-chain PII for a wallet address or email (GDPR right to erasure)
      description: >-
        Hard-deletes every row across off-chain tables matching the given identifier. Has no effect
        on-chain — see `docs/EVENT_SCHEMA.md` for the immutability and re-indexing caveats.
        Idempotent: re-running against an already-purged identifier is a safe no-op.
      tags:
        - Admin
      operationId: purgePiiForUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - identifier
              properties:
                identifier:
                  type: string
                  description: Wallet address (G...) or email to purge data for.
      responses:
        '200':
          description: Purge result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PiiPurgeResponse'
        '400':
          description: Missing identifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/pii/purge-campaign:
    post:
      summary: Erase campaign-scoped PII rows (referrals, allowlist entries, etc.)
      tags:
        - Admin
      operationId: purgePiiForCampaign
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - campaignId
              properties:
                campaignId:
                  type: string
      responses:
        '200':
          description: Purge result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PiiPurgeResponse'
        '400':
          description: Missing campaignId
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/audit-logs:
    get:
      summary: List audit logs
      tags:
        - Audit
      operationId: listAuditLogs
      parameters:
        - name: entity
          in: query
          description: Filter by entity type
          schema:
            type: string
        - name: entityId
          in: query
          description: Filter by entity ID
          schema:
            type: string
        - name: action
          in: query
          description: Filter by action
          schema:
            type: string
            enum: [create, update, delete]
        - name: page
          in: query
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
      responses:
        '200':
          description: Paginated audit log list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLogListResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/indexer/cursor:
    get:
      summary: Get indexer cursor state
      tags:
        - Config
      operationId: getIndexerCursor
      security: []
      responses:
        '200':
          description: Cursor state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IndexerCursorState'

    post:
      summary: Update indexer cursor
      tags:
        - Config
      operationId: setIndexerCursor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IndexerCursorUpdate'
      responses:
        '200':
          description: Cursor updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IndexerCursorState'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/organizations:
    post:
      summary: Create a new organization
      tags:
        - Organizations
      operationId: createOrganization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
              properties:
                name:
                  type: string
                slug:
                  type: string
                creatorEmail:
                  type: string
                  format: email
      responses:
        '201':
          description: Organization created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/organizations/{id}:
    get:
      summary: Get organization by ID
      tags:
        - Organizations
      operationId: getOrganization
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Organization details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
        '404':
          description: Organization not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    put:
      summary: Update organization
      tags:
        - Organizations
      operationId: updateOrganization
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                slug:
                  type: string
      responses:
        '200':
          description: Organization updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
        '404':
          description: Organization not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    delete:
      summary: Delete organization
      tags:
        - Organizations
      operationId: deleteOrganization
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Organization deleted
        '404':
          description: Organization not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/organizations/{id}/members:
    get:
      summary: List organization members
      tags:
        - Organizations
      operationId: listMembers
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: List of members
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/OrganizationMember'

  /api/v1/organizations/{id}/members/{memberId}:
    put:
      summary: Update member role
      tags:
        - Organizations
      operationId: updateMemberRole
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: memberId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - role
              properties:
                role:
                  type: string
                  enum: [owner, admin, member]
      responses:
        '200':
          description: Member role updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationMember'
        '400':
          description: Validation error or invalid operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Member not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    delete:
      summary: Remove member from organization
      tags:
        - Organizations
      operationId: removeMember
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: memberId
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Member removed
        '400':
          description: Invalid operation (e.g., removing last owner)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Member not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/organizations/{id}/invitations:
    post:
      summary: Create team invitation
      tags:
        - Organizations
      operationId: createInvitation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - role
              properties:
                email:
                  type: string
                  format: email
                role:
                  type: string
                  enum: [owner, admin, member]
      responses:
        '201':
          description: Invitation created
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                    enum: [invitation_created, invitation_exists, already_member, member_updated]
                  invitation:
                    $ref: '#/components/schemas/OrganizationInvitation'
                  inviteLink:
                    type: string
                  member:
                    $ref: '#/components/schemas/OrganizationMember'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        '404':
          description: Organization not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    get:
      summary: List organization invitations
      tags:
        - Organizations
      operationId: listInvitations
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, accepted, revoked, expired]
      responses:
        '200':
          description: List of invitations
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/OrganizationInvitation'

  /api/v1/organizations/{id}/invitations/{invitationId}:
    delete:
      summary: Revoke invitation
      tags:
        - Organizations
      operationId: revokeInvitation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: invitationId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Invitation revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  invitation:
                    $ref: '#/components/schemas/OrganizationInvitation'
        '400':
          description: Invalid operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          description: Invitation not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/organizations/invite/{token}:
    get:
      summary: Get invitation details by token
      tags:
        - Organizations
      operationId: getInvitationByToken
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Invitation details
          content:
            application/json:
              schema:
                type: object
                properties:
                  email:
                    type: string
                  role:
                    type: string
                  organization:
                    type: object
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                  status:
                    type: string
                  expiresAt:
                    type: string
                    format: date-time
        '404':
          description: Invitation not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /api/v1/organizations/invite/{token}/accept:
    post:
      summary: Accept invitation
      tags:
        - Organizations
      operationId: acceptInvitation
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
              properties:
                email:
                  type: string
                  format: email
      responses:
        '200':
          description: Invitation accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  type:
                    type: string
                  member:
                    $ref: '#/components/schemas/OrganizationMember'
                  organization:
                    $ref: '#/components/schemas/Organization'
        '400':
          description: Invalid invitation or validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  # ── #551: SEP-1 stellar.toml ──────────────────────────────────────────────
  /.well-known/stellar.toml:
    get:
      summary: SEP-1 stellar.toml token metadata
      tags: [StellarToml]
      security: []
      responses:
        '200':
          description: Valid SEP-1 TOML document
          headers:
            Cache-Control:
              schema: { type: string }
            Access-Control-Allow-Origin:
              schema: { type: string }
          content:
            text/plain:
              schema: { type: string }

  # ── #560: Index Read API ───────────────────────────────────────────────────
  /api/v1/index/campaigns/{id}/participants:
    get:
      summary: Cursor-paginated participant list from indexed balances
      tags: [IndexRead]
      security: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
        - in: query
          name: cursor
          schema: { type: string }
          description: Opaque pagination cursor from previous response
        - in: query
          name: limit
          schema: { type: integer, default: 20, maximum: 100 }
      responses:
        '200':
          description: Paginated participant list with freshness indicator
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        address: { type: string }
                        balance: { type: string }
                  cursor: { type: string, nullable: true }
                  has_more: { type: boolean }
                  as_of_ledger: { type: integer, nullable: true }
        '404':
          description: Campaign not found

  /api/v1/index/campaigns/{id}/events:
    get:
      summary: Cursor-paginated credit/claim event log for a campaign
      tags: [IndexRead]
      security: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
        - in: query
          name: type
          schema: { type: string, enum: [credit, claim] }
          description: Filter by event type
        - in: query
          name: cursor
          schema: { type: string }
        - in: query
          name: limit
          schema: { type: integer, default: 20, maximum: 100 }
      responses:
        '200':
          description: Paginated event list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        user: { type: string }
                        amount: { type: string }
                        ledger: { type: integer }
                        tx_hash: { type: string }
                        type: { type: string, enum: [credit, claim] }
                  cursor: { type: string, nullable: true }
                  has_more: { type: boolean }
                  as_of_ledger: { type: integer, nullable: true }
        '404':
          description: Campaign not found

  /api/v1/index/addresses/{address}/history:
    get:
      summary: Full event history for a single Stellar address
      tags: [IndexRead]
      security: []
      parameters:
        - in: path
          name: address
          required: true
          schema: { type: string }
        - in: query
          name: cursor
          schema: { type: string }
        - in: query
          name: limit
          schema: { type: integer, default: 20, maximum: 100 }
      responses:
        '200':
          description: Address event history and current balance
          content:
            application/json:
              schema:
                type: object
                properties:
                  address: { type: string }
                  balance: { type: string }
                  data: { type: array }
                  cursor: { type: string, nullable: true }
                  has_more: { type: boolean }
                  as_of_ledger: { type: integer, nullable: true }

  /api/v1/index/campaigns/{id}/stats:
    get:
      summary: Rollup stats for a campaign (ETag-cached)
      tags: [IndexRead]
      security: []
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Campaign stats
          content:
            application/json:
              schema:
                type: object
                properties:
                  campaignId: { type: string }
                  as_of_ledger: { type: integer, nullable: true }
                  summary:
                    type: object
                    properties:
                      totalParticipants: { type: integer }
                      totalCredited: { type: string }
                      totalClaimed: { type: string }
                      claimRate: { type: number }
        '304':
          description: Not Modified (ETag match)
        '404':
          description: Campaign not found

  # ── #556: Sponsored accounts ───────────────────────────────────────────────
  /api/v1/sponsored-accounts:
    post:
      summary: Sponsor a new Stellar account (CAP-33)
      tags: [SponsoredAccounts]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [address]
              properties:
                address:
                  type: string
                  description: Stellar G-address to sponsor
                accountType:
                  type: string
                  enum: [stellar, smart_wallet]
                  default: stellar
                trustlineAsset:
                  type: string
                  description: '"CODE:ISSUER" trustline to add within the sponsorship envelope'
      responses:
        '201':
          description: Sponsorship created; returns partially-signed transaction XDR
        '202':
          description: Sponsorship tracked (SPONSOR_SECRET_KEY not configured)
        '400':
          description: Invalid address or asset format
        '409':
          description: Address already has an active sponsorship

  /api/v1/sponsored-accounts/{address}:
    get:
      summary: Get sponsorship status for an address
      tags: [SponsoredAccounts]
      parameters:
        - in: path
          name: address
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Sponsorship record
        '404':
          description: Not found

  /api/v1/sponsored-accounts/{address}/revoke:
    post:
      summary: Revoke reserve sponsorship (transfer back to account)
      tags: [SponsoredAccounts]
      parameters:
        - in: path
          name: address
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Revocation XDR built; or tracked-only if key not configured
        '404':
          description: No active sponsorship for this address

  # ── #548: Claimable balances ───────────────────────────────────────────────
  /api/v1/campaigns/{id}/claimable-balances:
    post:
      summary: Trigger claimable balance creation for eligible-unclaimed users
      tags: [ClaimableBalances]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                graceDays:
                  type: integer
                  default: 30
                assetCode:
                  type: string
                assetIssuer:
                  type: string
      responses:
        '202':
          description: >-
            Submission enqueued on the durable job queue; returns { ok, campaignId, jobId, status:
            "queued" } immediately. Poll GET on this same path to see rows move pending ->
            created/failed.
        '404':
          description: Campaign not found
    get:
      summary: List claimable balances for a campaign
      tags: [ClaimableBalances]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
        - in: query
          name: status
          schema:
            {
              type: string,
              enum: [pending, created, claimed_by_user, reclaimed_by_operator, failed],
            }
        - in: query
          name: limit
          schema: { type: integer, default: 50, maximum: 200 }
        - in: query
          name: offset
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Paginated claimable balance list

  /api/v1/campaigns/{id}/claimable-balances/reclaim:
    post:
      summary: Operator reclaim of expired unclaimed balances
      tags: [ClaimableBalances]
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Balances marked as reclaimed
        '404':
          description: Campaign not found

components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
      description: API key for write operations
    BearerAuth:
      type: http
      scheme: bearer
      description: Bearer token authentication

  schemas:
    Campaign:
      type: object
      required:
        - id
        - name
        - slug
        - description
        - active
        - featured
        - rewardPerAction
        - createdAt
        - updatedAt
        - status
      properties:
        id:
          type: string
          description: Campaign ID
        name:
          type: string
          description: Campaign name
        slug:
          type: string
          description: URL-friendly slug
        description:
          type: string
          description: Campaign description
        active:
          type: boolean
          description: Whether campaign is active
        featured:
          type: boolean
          description: Whether campaign is featured
        hidden:
          type: boolean
          description: Whether campaign is hidden
        hiddenReason:
          type: string
          nullable: true
          description: Reason for hiding campaign
        rewardPerAction:
          type: number
          description: Reward points per action
        startDate:
          type: string
          format: date-time
          nullable: true
          description: Campaign start date
        endDate:
          type: string
          format: date-time
          nullable: true
          description: Campaign end date
        status:
          type: string
          enum: [draft, published, archived]
          description: Campaign publish-workflow status
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp
        updatedAt:
          type: string
          format: date-time
          description: Last update timestamp
        imageUrl:
          type: string
          format: uri
          nullable: true
          description: Public URL for campaign image
        tags:
          type: array
          items:
            type: string
          description: Campaign tags
        category:
          type: string
          nullable: true
          description: Campaign category

    CampaignCreate:
      type: object
      required:
        - name
        - rewardPerAction
      properties:
        name:
          type: string
          minLength: 1
          description: Campaign name
        slug:
          type: string
          description: URL-friendly slug (auto-generated if not provided)
        description:
          type: string
          description: Campaign description
        rewardPerAction:
          type: number
          minimum: 0
          description: Reward points per action
        active:
          type: boolean
          default: true
          description: Whether campaign is active
        featured:
          type: boolean
          default: false
          description: Whether campaign is featured
        hidden:
          type: boolean
          default: false
          description: Whether campaign is hidden
        hiddenReason:
          type: string
          nullable: true
          description: Reason for hiding campaign
        startDate:
          type: string
          format: date-time
          nullable: true
          description: Campaign start date
        endDate:
          type: string
          format: date-time
          nullable: true
          description: Campaign end date
        imageUrl:
          type: string
          format: uri
          description: Optional image URL
        tags:
          type: array
          maxItems: 10
          items:
            type: string
            maxLength: 32
        category:
          type: string
          description: Category from controlled vocabulary

    CampaignUpdate:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          description: Campaign name
        slug:
          type: string
          description: URL-friendly slug
        description:
          type: string
          description: Campaign description
        rewardPerAction:
          type: number
          minimum: 0
          description: Reward points per action
        active:
          type: boolean
          description: Whether campaign is active
        featured:
          type: boolean
          description: Whether campaign is featured
        hidden:
          type: boolean
          description: Whether campaign is hidden
        hiddenReason:
          type: string
          nullable: true
          description: Reason for hiding campaign
        startDate:
          type: string
          format: date-time
          nullable: true
          description: Campaign start date
        endDate:
          type: string
          format: date-time
          nullable: true
          description: Campaign end date

    CampaignListResponse:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Campaign'
        pagination:
          $ref: '#/components/schemas/Pagination'

    Pagination:
      type: object
      required:
        - total
        - count
        - page
        - limit
        - offset
        - totalPages
        - hasPreviousPage
        - hasNextPage
      properties:
        total:
          type: integer
          description: Total number of items
        count:
          type: integer
          description: Number of items in current page
        page:
          type: integer
          description: Current page number
        limit:
          type: integer
          description: Items per page
        offset:
          type: integer
          description: Number of items skipped
        totalPages:
          type: integer
          description: Total number of pages
        hasPreviousPage:
          type: boolean
          description: Whether there is a previous page
        hasNextPage:
          type: boolean
          description: Whether there is a next page
        previousPage:
          type: integer
          nullable: true
          description: Previous page number
        nextPage:
          type: integer
          nullable: true
          description: Next page number

    HealthResponse:
      type: object
      required:
        - status
        - service
        - timestamp
        - rpc
      properties:
        status:
          type: string
          enum: [ok, degraded]
          description: Overall health status
        service:
          type: string
          description: Service name
        timestamp:
          type: string
          format: date-time
          description: Health check timestamp
        rpc:
          $ref: '#/components/schemas/RpcHealthResponse'

    RpcHealthResponse:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          enum: [ok, error]
          description: RPC health status
        latency_ms:
          type: number
          description: RPC latency in milliseconds
        error:
          type: string
          description: Error message if status is error

    ApiInfo:
      type: object
      properties:
        name:
          type: string
        version:
          type: string
        prefix:
          type: string
        endpoints:
          type: object
        compatibility:
          type: object
        stellar:
          type: object
        config:
          type: object
        cors:
          type: object
        rateLimit:
          type: object
        body:
          type: object

    ConfigResponse:
      type: object
      required:
        - stellar
        - contracts
      properties:
        stellar:
          type: object
          properties:
            network:
              type: string
            networkPassphrase:
              type: string
            sorobanRpcUrl:
              type: string
            horizonUrl:
              type: string
            explorerUrl:
              type: string
        contracts:
          type: object
          properties:
            rewards:
              type: string
              nullable: true
            campaign:
              type: string
              nullable: true

    ExplorerResponse:
      type: object
      required:
        - network
        - explorerUrl
      properties:
        network:
          type: string
        explorerUrl:
          type: string

    AuditLogListResponse:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AuditLog'
        pagination:
          $ref: '#/components/schemas/Pagination'

    AuditLog:
      type: object
      required:
        - id
        - actor
        - action
        - entity
        - entityId
        - timestamp
      properties:
        id:
          type: string
        actor:
          type: string
        action:
          type: string
        entity:
          type: string
        entityId:
          type: string
        diff:
          type: object
        timestamp:
          type: string
          format: date-time

    IndexerCursorState:
      type: object
      required:
        - cursor
        - updatedAt
        - source
      properties:
        cursor:
          type: string
          nullable: true
        updatedAt:
          type: string
          format: date-time
        source:
          type: string

    IndexerCursorUpdate:
      type: object
      required:
        - cursor
      properties:
        cursor:
          type: string
          minLength: 1

    ApiKeyMetadata:
      type: object
      properties:
        id:
          type: string
        label:
          type: string
        orgId:
          type: string
          nullable: true
          description: Organization the key is scoped to, if any.
        scopes:
          type: array
          items:
            $ref: '#/components/schemas/ApiKeyScope'
          description: Granular permissions granted to the key.
        rateTier:
          $ref: '#/components/schemas/ApiKeyRateTier'
        createdAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
          nullable: true
        lastUsedAt:
          type: string
          format: date-time
          nullable: true
        active:
          type: boolean

    ApiKeyScope:
      type: string
      enum: [campaigns:read, campaigns:write, allowlist:write, admin]

    ApiKeyRateTier:
      type: string
      enum: [standard, pro, enterprise]
      description: >-
        Per-key rate-limit tier enforced by the rate limiter (#924). `standard` is 60 req/min, `pro`
        is 300 req/min, `enterprise` is 1000 req/min.

    PiiExportResponse:
      type: object
      properties:
        success:
          type: boolean
        identifier:
          type: string
        exportedAt:
          type: string
          format: date-time
        data:
          type: object
          description: >-
            One key per off-chain table with at least one matching row (empty tables are omitted).
            Values are arrays of the raw rows, except push_subscriptions.p256dh/auth which are
            redacted.
          additionalProperties:
            type: array
            items:
              type: object

    PiiPurgeResponse:
      type: object
      properties:
        success:
          type: boolean
        purged:
          type: array
          items:
            type: object
            properties:
              table:
                type: string
              count:
                type: integer

    Error:
      type: object
      required:
        - error
        - code
      properties:
        error:
          type: string
          description: Error message
        code:
          type: string
          description: Error code

    ValidationError:
      type: object
      required:
        - error
        - code
        - details
      properties:
        error:
          type: string
          description: Error message
        code:
          type: string
          description: Error code
        details:
          type: array
          items:
            type: string
          description: Validation error details

    Organization:
      type: object
      required:
        - id
        - name
        - slug
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
          description: Organization ID
        name:
          type: string
          description: Organization name
        slug:
          type: string
          description: URL-friendly slug
        createdAt:
          type: string
          format: date-time
          description: Creation timestamp
        updatedAt:
          type: string
          format: date-time
          description: Last update timestamp

    OrganizationMember:
      type: object
      required:
        - id
        - organizationId
        - userEmail
        - role
        - joinedAt
      properties:
        id:
          type: string
          description: Member ID
        organizationId:
          type: string
          description: Organization ID
        userEmail:
          type: string
          format: email
          description: Member email
        role:
          type: string
          enum: [owner, admin, member]
          description: Member role
        joinedAt:
          type: string
          format: date-time
          description: Join timestamp

    OrganizationInvitation:
      type: object
      required:
        - id
        - organizationId
        - email
        - role
        - token
        - invitedBy
        - invitedAt
        - expiresAt
        - status
      properties:
        id:
          type: string
          description: Invitation ID
        organizationId:
          type: string
          description: Organization ID
        email:
          type: string
          format: email
          description: Invitee email
        role:
          type: string
          enum: [owner, admin, member]
          description: Assigned role
        token:
          type: string
          description: Secure invitation token
        invitedBy:
          type: string
          description: Email of inviter
        invitedAt:
          type: string
          format: date-time
          description: Invitation timestamp
        expiresAt:
          type: string
          format: date-time
          description: Expiration timestamp
        acceptedAt:
          type: string
          format: date-time
          nullable: true
          description: Acceptance timestamp
        revokedAt:
          type: string
          format: date-time
          nullable: true
          description: Revocation timestamp
        status:
          type: string
          enum: [pending, accepted, revoked, expired]
          description: Invitation status

    CampaignStats:
      type: object
      required:
        - campaignId
        - onChainSynced
        - range
        - summary
        - registrationsByDay
        - pointsByDay
      properties:
        campaignId:
          type: string
          description: Campaign ID
        onChainSynced:
          type: boolean
          description: Whether on-chain data has been synced
        range:
          type: object
          properties:
            from:
              type: string
              format: date-time
            to:
              type: string
              format: date-time
        summary:
          type: object
          required:
            - totalParticipants
            - totalPoints
            - claimRate
            - timeRemainingMs
          properties:
            totalParticipants:
              type: integer
              description: Total registered participants
            totalPoints:
              type: integer
              description: Total points awarded
            claimRate:
              type: number
              description: Percentage of points claimed
            timeRemainingMs:
              type: integer
              description: Milliseconds until campaign ends
        registrationsByDay:
          type: array
          items:
            type: object
            properties:
              date:
                type: string
                format: date
              count:
                type: integer
          description: Daily registration counts
        pointsByDay:
          type: array
          items:
            type: object
            properties:
              date:
                type: string
                format: date
              credited:
                type: integer
              claimed:
                type: integer
          description: Daily credited and claimed points
